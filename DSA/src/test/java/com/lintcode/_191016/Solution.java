package com.lintcode._191016;

public class Solution {

	public int leastInterval(char[] tasks, int n) {
		if (tasks == null || tasks.length == 0)
			return -1;
		if (n == 0 || tasks.length == 1)
			return tasks.length;
		//先统计任务种类及其数量
		@SuppressWarnings("unused")
		int typeNum = 0;
		
		
		return n;
	}
}